"use strict";
let hobbies = ['sports', 'cooking'];
let users;
let possibleResults; // Tuple - restricted length if an arr
possibleResults = [1, 'string', true];
// possibleResults = [1, true] // Error
// possibleResults = [1, 2, 'fff'] //Error
let user = {
    name: 'dina',
    age: 20,
    hobbies: ['sport', 111],
    role: {
        description: 'some desription',
        id: 1
    }
};
let val = 'some text';
let data;
data = {
    entry1: 1,
    entry2: 'ss'
};
